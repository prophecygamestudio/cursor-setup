# Cursor Setup

One-line install for Windows:

**From PowerShell:**
```powershell
iex (irm 'https://raw.githubusercontent.com/RallyHereInteractive/cursor-setup/main/install.ps1')
```

**Or simply download and run `install.ps1` from this repository.**
